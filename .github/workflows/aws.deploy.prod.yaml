name: AWS Prod Deployment

on:
  workflow_dispatch:
  release:
    types: [published]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  AWS_HOST: 43.204.234.146  # 替换为生产环境IP
  API_HOST: 43.204.234.146  # 替换为生产环境IP

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Create environment file
      run: |
        echo "${{ secrets.ENV }}" > .env.production
        echo "📄 Environment configuration loaded from secrets"
        cat .env.production

    - name: Build application
      run: |
        # Use environment variables from .env.production file
        npx vite build --mode production

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=prod-latest,enable={{is_default_branch}}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

  deploy:
    runs-on: ubuntu-latest
    environment: PROD
    needs: build
    
    steps:
    - name: Deploy to AWS Server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ env.AWS_HOST }}
        username: root
        key: ${{ secrets.AWS_SSH_KEY }}
        port: 22
        timeout: 30s
        command_timeout: 10m
        script: |
          # 创建应用目录
          mkdir -p /root/inpayos-cashier-admin-prod/logs
          cd /root/inpayos-cashier-admin-prod
          
          # 登录到 GitHub Container Registry
          echo '${{ secrets.GITHUB_TOKEN }}' | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          
          # 停止并删除现有容器
          docker stop inpayos-cashier-admin-prod || true
          docker rm inpayos-cashier-admin-prod || true
          
          # 拉取最新镜像
          docker pull ghcr.io/ayhero/inpayos-cashier-admin:prod-latest
          
          # 创建环境配置文件
          echo '${{ secrets.ENV }}' > .env.production
          
          # 启动应用容器（nginx + 前端静态文件）
          docker run -d \
            --name inpayos-cashier-admin-prod \
            --restart unless-stopped \
            -p 3201:80 \
            -v /root/inpayos-cashier-admin-prod/logs:/var/log/nginx \
            -v /root/inpayos-cashier-admin-prod/.env.production:/app/.env.production \
            ghcr.io/ayhero/inpayos-cashier-admin:prod-latest
          
          # 等待服务启动
          sleep 15
          
          # 检查容器状态
          echo "🔍 Checking container status:"
          docker ps -a | grep inpayos-cashier-admin-prod
          
          # 检查应用是否启动成功
          if docker ps | grep inpayos-cashier-admin-prod | grep -q "Up"; then
            echo "✅ InPayOS Cashier Admin frontend (PROD) is running"
            
            # 检查端口监听
            echo "🔍 Checking port status:"
            netstat -tlnp | grep ":3201" || echo "Port 3201 may still be binding..."
            
            # 尝试健康检查
            echo "🏥 Health check:"
            if curl -f http://localhost:3201 2>/dev/null; then
              echo "✅ Cashier Admin frontend health check passed"
            else
              echo "⚠️ Cashier Admin frontend health check failed (may still be starting)"
            fi
            
            # 显示最近日志
            echo "📝 Recent logs:"
            docker logs --tail=20 inpayos-cashier-admin-prod
          else
            echo "❌ InPayOS Cashier Admin container failed to start"
            docker logs inpayos-cashier-admin-prod
            exit 1
          fi