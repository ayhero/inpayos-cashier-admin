name: AWS Dev Deployment

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  AWS_HOST: 43.204.234.146
  API_HOST: 43.204.234.146
  CASHIER_ADMIN_API_PORT: 6084

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: DEV
    permissions:
      contents: read
      packages: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build application
      run: |
        export VITE_API_URL=http://${{ env.API_HOST }}:${{ env.CASHIER_ADMIN_API_PORT }}
        # Skip TypeScript check for now and build directly with Vite
        npx vite build --mode production

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

    - name: Deploy to AWS Server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ env.AWS_HOST }}
        username: root
        key: ${{ secrets.AWS_SSH_KEY }}
        port: 22
        timeout: 30s
        command_timeout: 10m
        script: |
          # 创建应用目录
          mkdir -p /root/inpayos-cashier-admin/logs
          cd /root/inpayos-cashier-admin
          
          # 登录到 GitHub Container Registry
          echo '${{ secrets.GITHUB_TOKEN }}' | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          
          # 停止并删除现有容器
          docker stop inpayos-cashier-admin || true
          docker rm inpayos-cashier-admin || true
          
          # 拉取最新镜像
          docker pull ghcr.io/ayhero/inpayos-cashier-admin:latest
          
          # 启动应用容器（nginx + 前端静态文件）
          docker run -d \
            --name inpayos-cashier-admin \
            --restart unless-stopped \
            -p 8081:80 \
            -v /root/inpayos-cashier-admin/logs:/var/log/nginx \
            ghcr.io/ayhero/inpayos-cashier-admin:latest
          
          # 等待服务启动
          sleep 15
          
          # 检查容器状态
          echo "🔍 Checking container status:"
          docker ps -a | grep inpayos-cashier-admin
          
          # 检查应用是否启动成功
          if docker ps | grep inpayos-cashier-admin | grep -q "Up"; then
            echo "✅ InPayOS Cashier Admin frontend is running"
            
            # 检查端口监听
            echo "🔍 Checking port status:"
            netstat -tlnp | grep ":8081" || echo "Port 8081 may still be binding..."
            
            # 尝试健康检查
            echo "🏥 Health check:"
            if curl -f http://localhost:8081 2>/dev/null; then
              echo "✅ Cashier Admin frontend health check passed"
            else
              echo "⚠️ Cashier Admin frontend health check failed (may still be starting)"
            fi
            
            # 显示最近日志
            echo "📝 Recent logs:"
            docker logs --tail=20 inpayos-cashier-admin
          else
            echo "❌ InPayOS Cashier Admin container failed to start"
            docker logs inpayos-cashier-admin
            exit 1
          fi
